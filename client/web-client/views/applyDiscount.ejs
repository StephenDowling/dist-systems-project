<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>

    <a href="/">Add To Cart</a>
    <a href="/removeFromCart">Remove From Cart</a>
    <a href="/totalValue">Total Value</a>
    <a href="/applyDiscount">Apply Discount</a>
    <a href="/processPayment">Process Payment</a>

    <% if(error) { %>
      <%= error %>
    <% } %>

    <form action="/applyDiscount" method="get">

    </form>

    <% if(!isNaN(total)) { %>
      <b>Your new total is: €</b><%= total %>
    <% } %>

    <% if (typeof discountConf === 'string') { %>
    <b>ANS:</b><%= discountConf %>
<% } %>


  </body>
</html>
